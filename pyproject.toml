[project]
name = "streaming-recipe-chatbot"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.12",
    "python-dotenv>=1.1.0",
    "together>=1.5.8",
    "uvicorn>=0.34.2",
]
